# iSongly frontend

React + Vite UI for the iSongly song library. See the [project README](../README.md) for the full architecture and how to run the backend.

```bash
npm install
npm run dev
```

Talks to the backend at `http://localhost:8080` by default; override with `VITE_API_BASE_URL`.
